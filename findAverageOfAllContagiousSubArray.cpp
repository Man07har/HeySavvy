/* Given an array, find the average of all contiguous subarrays of size ‘K’ in it. */

//brute force Approach

class solution {
    public:
    int averageOfSubArray(int arr[]){
        int k;
        int sum=0;
        for(int i=0;i<arr.size();i++)
        { 
            sum+=arr[i];
            for(int j=i;j<i+1;j++){
                
            }
        }
    }
}